#spring.application.name=ms-config-server
server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kennyaapro23/proyectoDs.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
    security:
      user:
        name: root
        password: 123456
