server:
  port: 7070

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/kennyaapro23/proyectoDs.git
          search-paths: config-data
          #username:
          #password:
          default-label: main
  security:
    user:
      name: root
      password: 123456